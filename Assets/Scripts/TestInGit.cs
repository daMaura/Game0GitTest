using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TestInGit : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        print("hallo liebe kolegen");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
